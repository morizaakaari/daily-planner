name: routine_master_app
description: Daily planner, twitter sprint tracker, and AI course finder
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  http: ^1.2.0

flutter:
  uses-material-design: true
